const http = require('http');
const fs = require('fs');
const path = require('path');

const pathToIndex = path.join(__dirname, 'static','index.html');
const IndexHtmlFile = fs.readFileSync(pathToIndex);

const pathToStyle = path.join(__dirname, 'static','css.css');
const CssHtmlFile = fs.readFileSync(pathToStyle);

const pathToScript = path.join(__dirname, 'static','index.js');
const IndexHtmlFile = fs.readFileSync(pathToScript);

const server = http.createServer((req,res)=>{ 
    switch (req.url) {
		case '/'return res.end(IndexHtmlFile);
		case '/css.css': return  res.end(CssHtmlFile);
		case '/index.js': return  res.end(IndexHtmlFile);
	}
	res.statusCode == 404;
	return res.end("Error 404");

});
server.listen(3000);






























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































